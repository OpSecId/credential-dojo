# Placeholders replaced at container start (see docker-entrypoint.sh).
# __LISTEN_PORT__ — Railway sets PORT; local Compose defaults to 80.
server {
    listen __LISTEN_PORT__;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    location /api/ {
        proxy_pass __API_UPSTREAM__;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # OID4VCI process-offer chains several HTTPS hops; defaults (often 60s) can 504 before Node responds.
        proxy_connect_timeout 75s;
        proxy_send_timeout 180s;
        proxy_read_timeout 180s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
